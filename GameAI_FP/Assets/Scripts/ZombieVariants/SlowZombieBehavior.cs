using UnityEngine;

// the default zombie is the slow zombie so just the zombie controller defaults! 
public class SlowZombieBehavior : ZombieVariantBehavior
{
    // same as the zombiecontroller default so nothing here
}